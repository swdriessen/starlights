{
  "name": "starlights",
  "private": true,
  "workspaces": [
    "builder-app",
    "libs/ui",
    "packages/ui-framework",
    "apps/content-manager"
  ],
  "scripts": {
    "dev": "npm run dev -w builder-app",
    "devcm": "npm run dev -w apps/content-manager",
    "build": "npm run build -w packages/ui-framework && npm run build -w builder-app && npm run build -w apps/content-manager",
    "lint": "npm run lint -w builder-app",
    "preview": "npm run preview -w builder-app"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "lucide-react": "^1.8.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
