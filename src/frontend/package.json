{
  "name": "starlights",
  "private": true,
  "workspaces": [
    "packages/ui",
    "packages/ui-framework",
    "apps/builder-app",
    "apps/content-manager",
    "apps/showcase"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/builder-app",
    "devcm": "npm run dev -w apps/content-manager",
    "showcase": "npm run dev -w apps/showcase",
    "build": "npm run build -w packages/ui-framework && npm run build -w apps/showcase && npm run build -w apps/builder-app && npm run build -w apps/showcase",
    "lint": "npm run lint -w apps/builder-app",
    "preview": "npm run preview -w apps/builder-app"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "lucide-react": "^1.8.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
