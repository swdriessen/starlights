namespace Starlights.Platform.Data;

/// <summary>
/// Represents a context for persistence operations. The concrete implementation will depend on the underlying storage technology (e.g., Entity Framework).
/// </summary>
public interface IPersistenceContext;
